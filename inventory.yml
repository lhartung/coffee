aws:
  hosts:
    coffee.lhartung.com:
      ansible_user: ubuntu

      containers:
        - homemealplan
        - influxdb2
        - mariadb
        - myfreezer
        - vikunja
        - wordpress

      volume_directory: /var/volumes

      webroot: /var/www/html
